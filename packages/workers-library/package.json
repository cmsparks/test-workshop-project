{
	"name": "@repo/workers-library",
	"version": "0.0.0",
	"private": true,
	"sideEffects": false,
	"type": "module",
	"main": "./src/index.ts",
	"scripts": {
		"check:lint": "eslint . --max-warnings=0 --cache --cache-strategy content --cache-location ./node_modules/.cache/eslint",
		"check:types": "tsc -p tsconfig.json",
		"test": "vitest run",
		"test:watch": "vitest",
		"build:mocks": "wrangler deploy --minify --outdir ./dist --dry-run --name 'mock-ai' --compatibility-date '2024-03-12' ./src/test/ai.mock.ts"
	},
	"devDependencies": {
		"@cloudflare/types": "^6.29.1",
		"@cloudflare/vitest-pool-workers": "^0.4.23",
		"@cloudflare/workers-types": "^4.20240815.0",
		"@repo/eslint-config": "workspace:*",
		"@repo/typescript-config": "workspace:*",
		"@vitest/ui": "1.5.3",
		"vitest": "1.5.3"
	}
}